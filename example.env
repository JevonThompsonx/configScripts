# ClamAV Telegram Bot Configuration
#
# INSTRUCTIONS:
# 1. Copy this file to your home directory: cp example.env ~/.clamav-telegram.env
# 2. Fill in your actual Telegram bot token and chat ID below
# 3. Secure the file: chmod 600 ~/.clamav-telegram.env
# 4. Run the setup script: bash setup-clamav-cronjob.sh
#
# HOW TO GET THESE VALUES:
#
# Telegram Bot Token:
#   1. Open Telegram and search for @BotFather
#   2. Send /newbot and follow the prompts
#   3. Copy the token provided (format: 1234567890:ABCdefGHIjklMNOpqrsTUVwxyz)
#
# Telegram Chat ID:
#   1. Search for @userinfobot in Telegram
#   2. Start a chat and it will send you your Chat ID
#   3. Copy your ID (format: 1234567890)
#
#   OR for group notifications:
#   1. Add your bot to a group
#   2. Send a message in the group
#   3. Visit: https://api.telegram.org/bot<YOUR_BOT_TOKEN>/getUpdates
#   4. Look for "chat":{"id":-1234567890} - that's your group chat ID
#

# Required: Your Telegram bot token from @BotFather
TELEGRAM_BOT_TOKEN=your_bot_token_here

# Required: Your Telegram chat ID (user ID or group chat ID)
TELEGRAM_CHAT_ID=your_chat_id_here

# Deep Scan Configuration (clamav-scan.sh - runs at 8 PM)
# Directory for full system scan (default: /home)
# You can scan your entire system with "/" but it will take much longer
SCAN_DIR=/home

# Fast Scan Configuration (clamav-scan-fast.sh - runs at 5 AM)
# Space-separated list of critical directories for quick morning scan
# Recommended: locations where new files are most likely to appear
FAST_SCAN_TARGETS="$HOME/Documents $HOME/Downloads $HOME/Desktop $HOME/scripts"

# Optional: Custom log directory (default: $HOME/.clamav-logs)
# LOG_DIR=/var/log/clamav-scans

# Optional: Use clamdscan (daemon) for faster scanning (default: false)
# Requires clamd to be running (setup script configures this)
# Benefits: 10-50x faster, lower CPU usage
USE_CLAMDSCAN=false
